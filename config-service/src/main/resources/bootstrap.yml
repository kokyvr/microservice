server:
  port: 8081

spring:  cloud:    config:      server:        git:          uri: https://github.com/kokyvr/microservice.git
          search-paths: config-data
          default-label: master
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:    user:      name: root
      password: s3cret