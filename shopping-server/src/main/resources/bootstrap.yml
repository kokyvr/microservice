spring:
  application:
    name: shopping-service
  cloud:
    config:
      uri:
      - http://localhost:8081
      username: root
      password: s3cret
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/